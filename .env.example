# Variables de entorno para Spikes CRM Unificado

# Para desarrollo local
NEXT_PUBLIC_API_URL=/api
NEXT_PUBLIC_URL_CONTRACT=http://localhost:4000

# JWT Secret (generar uno seguro para producción)
JWT_SECRET=tu-jwt-secret-super-seguro-aqui-256-bits-minimo

# Base de datos PostgreSQL
# Para desarrollo local (usar Docker o PostgreSQL local)
POSTGRES_URL=postgresql://postgres:password@localhost:5432/spikes_dev

# Para producción en Vercel (se configura automáticamente)
# POSTGRES_URL=postgresql://... (Vercel Postgres)
# POSTGRES_PRISMA_URL=postgresql://... (Vercel Postgres con pooling)
# POSTGRES_URL_NON_POOLING=postgresql://... (Vercel Postgres sin pooling)

# Variables de producción
# NEXT_PUBLIC_API_URL=/api
# NEXT_PUBLIC_URL_CONTRACT=https://tu-dominio.com
# JWT_SECRET=secure-production-jwt-secret-256-bits
# NODE_ENV=production

# Opcional: AWS S3 para subida de archivos
# AWS_ACCESS_KEY_ID=tu-access-key
# AWS_SECRET_ACCESS_KEY=tu-secret-key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=tu-bucket-name

# Opcional: Email service (Resend)
# RESEND_API_KEY=tu-resend-api-key